"""
Library utils
"""


def explore_paths(schema):
    """
    Explore a schema and extract its features path recursively
    """

    # if schema['type'] == 'object':

    #     for f in schema["allOf"][0]["properties"]:
    pass

 
